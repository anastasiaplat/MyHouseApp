<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyHouseApp0" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white_green</item>
        <item name="android:statusBarColor">@color/navbar</item>
        <item name="android:buttonTint">@color/white_green</item>
    </style>
    <style name="Theme.MyHouseApp0" parent="Base.Theme.MyHouseApp0" />
</resources>